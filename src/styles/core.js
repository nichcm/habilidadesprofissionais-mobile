import react from "react";
import { StyleSheet  } from 'react-native'

const core = StyleSheet.create({
    bgLogin: {
        flex:1,
      },
    imgLogin: {
        height: 108,
        width: 106,

    },

});

export default core

